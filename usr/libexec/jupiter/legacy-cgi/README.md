# Legacy CGI

These executables are kept only as a migration reference.

The production web service is Node.js/Express and exposes compatible `/cgi-bin/...` HTTP routes from `/www/jupiter/server.js`.
